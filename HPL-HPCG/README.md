# OPTIMIZED RUN SCRIPTS FOR HPL AND HPCG

The included scripts are optimized for a specific GPU cluster, so the flags are not universal. HPL and HPCG are deployed using NVIDIA's HPC-Benchmark container version 26.02.

Regarding optimizations, there is no source-code kernel changes, only system aware flag-tuning.

Note: The runs are for SINGLE node only.
