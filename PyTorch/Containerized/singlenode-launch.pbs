#PBS -N 1x1GPU_DDP
#PBS -q gpu_as
#PBS -P hpc_admin

# ------------------------------------------------------------
# IMPORTANT:
# Request 2 GPUs on 1 node
#
# To change GPU count later:
# ngpus=4  --> request 4 GPUs
# ngpus=8  --> request 8 GPUs
# ------------------------------------------------------------
#PBS -l select=1:ngpus=1

#PBS -l walltime=08:00:00

cd $PBS_O_WORKDIR

echo "===== Job Info ====="
echo "Job ID: $PBS_JOBID"

echo "Running on node:"
hostname
echo

echo "===== GPU Info ====="
nvidia-smi
echo

# ------------------------------------------------------------
# Load Modules
# ------------------------------------------------------------
module load apptainer/1.4.1
module load squashfuse/0.5.2
module load gocryptfs/2.5.0

# ------------------------------------------------------------
# Paths
# ------------------------------------------------------------
container_path="/home/pham0094/pyTorch_container/pytorch_26.04-py3.sif"

# IMPORTANT:
# Use the NEW DDP Python script
python_script="resnet50_singleNode_DDP.py"

# ------------------------------------------------------------
# Container PyTorch Info
# ------------------------------------------------------------
echo "===== Container PyTorch Info ====="

apptainer exec --nv "$container_path" python -c "
import torch
print('PyTorch:', torch.__version__)
print('CUDA available:', torch.cuda.is_available())
print('CUDA version:', torch.version.cuda)
print('GPU count:', torch.cuda.device_count())

for i in range(torch.cuda.device_count()):
    print(f'GPU {i}:', torch.cuda.get_device_name(i))
"

echo

# ------------------------------------------------------------
# IMPORTANT:
# Launch DDP with torchrun
#
# --nproc_per_node = number of GPUs/processes
#
# To scale later:
# --nproc_per_node=4
# --nproc_per_node=8
# ------------------------------------------------------------
echo "===== Running Multi-GPU DDP Benchmark ====="

apptainer exec --nv "$container_path" \
torchrun --nproc_per_node=1 "$python_script"
